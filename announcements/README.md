Create announcements to send to the course by creating a markdown file in this folder.

Use hosted image URLs, local relative image paths, and embed video code. Local images are uploaded to Canvas when you run the announcement creation scripts.
